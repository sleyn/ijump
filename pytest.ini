[pytest]
testpaths = tests
markers =
    e2e: end-to-end characterization goldens; needs the large inputs in Test/ (or $IJUMP_E2E_DATA) and BLAST+, skips without them, and takes a few minutes. Deselect with -m "not e2e".
